PartyKit entrypoint is [server.ts](/D:/WH-workspace/tetris/tetris-web/partykit/server.ts).

Recommended next steps:

1. Install PartyKit packages:
   `npm install -D partykit`
2. Add a config file if needed:
   `partykit.json`
3. Run local PartyKit dev server:
   `npx partykit dev partykit/server.ts`
4. Point the web client `host` to the deployed PartyKit host and use `useRoomConnection`.
